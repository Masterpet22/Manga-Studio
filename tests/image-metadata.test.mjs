import test from'node:test';import assert from'node:assert/strict';import{applyDpiMetadata}from'../src/image-metadata.js';
test('adds a PNG pHYs chunk with requested density',()=>{const png=new Uint8Array([...([137,80,78,71,13,10,26,10]),0,0,0,13,73,72,68,82,...new Uint8Array(17)]),result=applyDpiMetadata(png,'image/png',300);assert.equal(result.length,png.length+21);assert.equal(new TextDecoder().decode(result.subarray(37,41)),'pHYs');assert.equal(new DataView(result.buffer,result.byteOffset+41,4).getUint32(0),Math.round(300/0.0254))});
test('updates JFIF density for JPEG output',()=>{const jpeg=new Uint8Array([255,216,255,224,0,16,74,70,73,70,0,1,1,0,1,0,1,0,0]),result=applyDpiMetadata(jpeg,'image/jpeg',300);assert.equal(result[13],1);assert.equal((result[14]<<8)|result[15],300);assert.equal((result[16]<<8)|result[17],300)});
